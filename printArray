let prompt=require('prompt-sync')();
let n=Number(prompt('enrer size of array : '))
let arr=new Array(n)

for(let i=0;i<arr.length;i++){
    arr[i]=Number(prompt('enter a value : '))
}
console.log(arr);





